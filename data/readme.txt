README.TXT

The files in this directory are xml data files used ONLY when using XML as the backend
storage mechanism. By default the storage mechanism is database. If you are using a
database then you don't need to worry about the files in this directory.